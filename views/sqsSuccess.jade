extends layout

block content
    script(type='text/javascript').
        $(document).ready(function () {
            // Handler for .ready() called.
            window.setTimeout(function () {
                location.href = "/pictureGallery";
            }, 5000);
        });
    h3 Sepia effect is being applied... You will be redirected to gallery within 5 s.